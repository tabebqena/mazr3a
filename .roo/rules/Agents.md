For each plan, you should create a plan file
After implementing a plan, you should record what is implemented in the same plan file.
After Each changes, you should write a git commit.

Never Edit the AI rules by yourself, ask the user to edit it.