# scenewatch runtime dependencies (image only).
# Keep minimal: VLM inference (OpenVINO GenAI + OpenVINO runtime) on CPU,
# JPEG decode (Pillow) and the numpy-only motion mask maths.
# HTTP to Frigate and the SQLite store use the Python standard library.
#
# openvino-genai pulls in openvino + openvino-tokenizers, but they are
# listed explicitly so the pin is visible and reproducible.
openvino>=2024.4
openvino-genai>=2024.4
openvino-tokenizers>=2024.4
numpy>=1.24,<3
Pillow>=9.1,<12
